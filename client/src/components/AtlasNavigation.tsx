import { useAuth } from "@/_core/hooks/useAuth";
import { ATLAS_GROUP_LABELS, ATLAS_MODULES, type AtlasModuleGroup } from "@shared/atlas-modules";
import { BarChart3, BookOpenCheck, Database, Home, Landmark, LogIn, LogOut, Scale, Settings2, ShieldCheck, Sparkles, type LucideIcon } from "lucide-react";
import { useLocation } from "wouter";
const icons:Record<string,LucideIcon>={home:Home,compendium:BookOpenCheck,theses:Sparkles,jurimetryJec:BarChart3,national:Landmark,metropolitan:Database,sources:Database,governance:ShieldCheck,control:Settings2,evidenceControl:Database,thesisCuration:Scale};
const groups:AtlasModuleGroup[]=["intelligence","jurimetry","governance"];
function active(location:string,route:string){return route==="/"?location==="/":location===route||location.startsWith(`${route}/`);}
export default function AtlasNavigation(){const {user,login,logout}=useAuth();const [location]=useLocation();const visible=ATLAS_MODULES.filter(m=>m.access!=="admin"||user?.role==="admin");return <header className="site-header"><div className="header-inner"><a href="/" className="brand"><span className="brand-mark"><Scale size={17}/></span><span><strong>Atlas Forense</strong><small>Inteligência jurídica auditável</small></span></a><div className="header-actions"><span className="source-authority"><b>Fonte autoritativa</b><small>Compêndio · Teses · Jurimetria</small></span>{user?<button className="ghost-button" onClick={()=>void logout()}><LogOut size={14}/> Sair</button>:<button className="ghost-button" onClick={login}><LogIn size={14}/> Entrar</button>}</div></div><nav className="module-nav" aria-label="Módulos do Atlas">{groups.flatMap(group=>visible.filter(m=>m.group===group).map(m=>{const Icon=icons[m.key]??Scale;return <a key={m.key} href={m.route} className={active(location,m.route)?"active":""} title={`${ATLAS_GROUP_LABELS[group]} · ${m.description}`}><Icon size={14}/>{m.shortLabel}</a>}))}</nav></header>}
