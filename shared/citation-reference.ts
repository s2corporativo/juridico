export function buildPublicCitationReference(input:{theme?:string|null;tribunal:string;cnjNumber?:string|null;externalId:string;decisionDate?:Date|string|null;sourceUrl?:string|null}){const date=input.decisionDate?new Intl.DateTimeFormat("pt-BR").format(new Date(input.decisionDate)):"data não informada";return `${input.tribunal}. ${input.theme??"Decisão judicial"}. ${input.cnjNumber??input.externalId}. ${date}.${input.sourceUrl?` Fonte: ${input.sourceUrl}`:""}`}
