export type NormalizedOutcome="favoravel"|"desfavoravel"|"parcial"|"acordo"|"extincao"|"indeterminado";
export function parseBrazilianCurrencyToCents(input:string|number|null|undefined){if(input===null||input===undefined||input==="")return null;if(typeof input==="number")return Number.isFinite(input)?Math.round(input*100):null;const cleaned=input.replace(/R\$|\s/g,"").replace(/\./g,"").replace(",",".").replace(/[^0-9.-]/g,"");const value=Number(cleaned);return Number.isFinite(value)?Math.round(value*100):null}
export function normalizeOutcome(value:string|null|undefined):NormalizedOutcome{const text=(value??"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase();if(!text)return"indeterminado";if(/acordo|concili/.test(text))return"acordo";if(/extint|arquiv|sem resolucao/.test(text))return"extincao";if(/parcial|em parte/.test(text))return"parcial";if(/improced|desfavor|negado|nao provido|não provido/.test(text))return"desfavoravel";if(/proced|favor|provido|acolhido/.test(text))return"favoravel";return"indeterminado"}
export function slugDisposition(value:string|null|undefined){const slug=(value??"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");return slug||null}
