import{describe,expect,it}from"vitest";import{isAllowedPublicStorageKey}from"./_core/storageProxy";describe("storage allowlist",()=>{it("aceita somente prefixo explícito",()=>{expect(isAllowedPublicStorageKey("atlas-forense-seal_/x.pdf",["atlas-forense-seal_"])).toBe(true);expect(isAllowedPublicStorageKey("other/x.pdf",["atlas-forense-seal_"])).toBe(false)});it("rejeita traversal e percent encoding",()=>{expect(isAllowedPublicStorageKey("atlas-forense-seal_/../secret",["atlas-forense-seal_"])).toBe(false);expect(isAllowedPublicStorageKey("atlas-forense-seal_/%2e%2e/x",["atlas-forense-seal_"])).toBe(false)})});
