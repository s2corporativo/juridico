import{z}from"zod";import{adminProcedure,publicProcedure,router}from"../_core/trpc";import{decideEvidenceReview,enqueueEvidenceReview,getCitationDossier,getCompendiumFreshnessOverview,getCompendiumOverview,getCompendiumQualityOverview,getEvidenceReviewQueue,searchCompendium}from"../db";import{previewControlledIngestion}from"../compendium.ingestion";
const sourceStatus=z.enum(["official_confirmed","official_without_number","attachment_reviewed","secondary_pending","movement_observed","search_thematic"]);export const compendiumRouter=router({overview:publicProcedure.query(()=>getCompendiumOverview()),quality:publicProcedure.query(()=>getCompendiumQualityOverview()),freshness:publicProcedure.query(()=>getCompendiumFreshnessOverview()),search:publicProcedure.input(z.object({query:z.string().trim().max(300).optional(),tribunal:z.string().trim().max(64).optional(),city:z.string().trim().max(128).optional(),legalArea:z.string().trim().max(255).optional(),sourceStatus:sourceStatus.optional(),page:z.number().int().min(0).max(10000).optional(),pageSize:z.number().int().min(1).max(50).optional()})).query(({input})=>searchCompendium(input)),dossier:publicProcedure.input(z.object({externalId:z.string().trim().min(1).max(191)})).query(({input})=>getCitationDossier(input.externalId)),reviewQueue:router({list:adminProcedure.input(z.object({status:z.enum(["pending","approved","rejected","returned"]).optional(),priority:z.enum(["routine","elevated","urgent"]).optional(),tribunal:z.string().trim().max(191).optional()}).optional()).query(({input})=>getEvidenceReviewQueue(input)),enqueue:adminProcedure.input(z.object({externalId:z.string().trim().min(1).max(191),priority:z.enum(["routine","elevated","urgent"]),requestedReason:z.string().trim().min(3).max(2000)})).mutation(({ctx,input})=>enqueueEvidenceReview(input.externalId,input.priority,input.requestedReason,ctx.user.userId)),decide:adminProcedure.input(z.object({reviewId:z.number().int().positive(),decision:z.enum(["approved","rejected","returned"]),decisionNote:z.string().trim().min(3).max(2000)})).mutation(({ctx,input})=>decideEvidenceReview(input.reviewId,input.decision,input.decisionNote,ctx.user.userId))}),ingestion:router({preview:adminProcedure.input(z.object({batchKey:z.string().trim().min(3).max(191),candidates:z.array(z.object({externalId:z.string().trim().min(1).max(191),cnjNumber:z.string().trim().max(80).optional(),tribunal:z.string().trim().min(1).max(64),justice:z.string().trim().min(1).max(64),decisionType:z.string().trim().min(1).max(64),sourceUrl:z.string().url().max(1024).optional(),sourceStatus,metadata:z.record(z.string(),z.unknown()).optional()})).min(1).max(200)})).mutation(({input})=>previewControlledIngestion(input.batchKey,input.candidates))})});
