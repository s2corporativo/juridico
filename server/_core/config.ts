import{ENV,isProduction}from"./env";
function pair(label:string,a:string,b:string){if(Boolean(a)!==Boolean(b))throw new Error(`${label}: configuração incompleta.`)}
function trio(label:string,a:string,b:string,c:string){const n=[a,b,c].filter(Boolean).length;if(n>0&&n<3)throw new Error(`${label}: issuer/client-id/client-secret devem ser definidos juntos.`)}
export function validateRuntimeConfig(){if(!Number.isInteger(ENV.port)||ENV.port<1||ENV.port>65535)throw new Error("PORT inválida.");pair("Forge storage",ENV.forgeApiUrl,ENV.forgeApiKey);trio("OIDC Atlas",ENV.oidcIssuer,ENV.oidcClientId,ENV.oidcClientSecret);trio("OIDC EJC",ENV.ejcOidcIssuer,ENV.ejcOidcClientId,ENV.ejcOidcClientSecret);if(ENV.forgeApiUrl&&!ENV.publicStoragePrefixes.length)throw new Error("Storage remoto exige PUBLIC_STORAGE_PREFIXES.");if(isProduction){if(ENV.port!==3010)throw new Error("Produção deve usar PORT=3010.");if(ENV.host!=="127.0.0.1")throw new Error("Produção deve usar HOST=127.0.0.1 atrás do reverse proxy.");if(!ENV.databaseUrl)throw new Error("DATABASE_URL é obrigatória em produção.");if(ENV.jwtSecret.length<64)throw new Error("JWT_SECRET deve possuir no mínimo 64 caracteres em produção.")}return{ok:true as const}}
